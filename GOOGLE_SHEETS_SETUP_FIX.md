# 🔧 แก้ไขปัญหา Google Sheets Sync

## ❌ ปัญหาที่พบ

1. **Google Sheets API ยังไม่ได้เปิดใช้งาน** (Error 403)
2. **รูปแบบวันที่ต้องแปลงจาก DD/MM/YYYY เป็น YYYY-MM-DD**

---

## ✅ วิธีแก้ไข

### ขั้นตอนที่ 1: เปิดใช้งาน Google Sheets API

1. **เปิดลิงก์นี้ในเบราว์เซอร์:**
   ```
   https://console.developers.google.com/apis/api/sheets.googleapis.com/overview?project=725357812362
   ```

2. **คลิกปุ่ม "ENABLE" (เปิดใช้งาน)**
   - หากยังไม่ได้ล็อกอิน ให้ล็อกอินด้วย Google Account ที่สร้าง Service Account
   - รอสักครู่ให้ระบบเปิดใช้งาน API (อาจใช้เวลา 1-2 นาที)

3. **ตรวจสอบว่า API เปิดใช้งานแล้ว:**
   ```powershell
   node test-google-sheets.js
   ```
   
   ผลลัพธ์ที่ถูกต้องควรจะเป็น:
   ```
   ✓ Authorization successful
   ✓ Sheet found: [ชื่อชีต]
   Available sheets:
     1. สาวอ้อย
     2. อลิน
     ...
   ```

### ขั้นตอนที่ 2: แชร์ Google Sheet ให้ Service Account

1. **เปิด Google Sheet:**
   ```
   https://docs.google.com/spreadsheets/d/1Q8aPPsro7_7aqWNs13kPa3OgFp7qDRmlnXY7ve3B9hw
   ```

2. **คลิกปุ่ม "Share" (แชร์) มุมขวาบน**

3. **เพิ่ม Email นี้:**
   ```
   thailand-sh0424@sa-ads.iam.gserviceaccount.com
   ```

4. **เลือกสิทธิ์: "Viewer" (ดูอย่างเดียว)**

5. **คลิก "Send" หรือ "Done"**

---

## 🔄 การแปลงวันที่

ระบบได้ถูกปรับปรุงแล้วเพื่อแปลงวันที่จาก Google Sheets (DD/MM/YYYY) ไปเป็นรูปแบบ MySQL (YYYY-MM-DD)

### ตัวอย่าง:
- **Google Sheets:** `25/12/2024`
- **MySQL:** `2024-12-25` (DateTime)

### โค้ดที่ถูกอัปเดต:

**ไฟล์:** `src/lib/google-sheets.ts`

```typescript
// ฟังก์ชันช่วยแปลงวันที่จาก DD/MM/YYYY เป็น YYYY-MM-DD
const toDate = (val: any): Date => {
  if (!val) return new Date();
  
  // ถ้าเป็นรูปแบบ DD/MM/YYYY (จาก Google Sheets)
  if (typeof val === 'string' && val.includes('/')) {
    const parts = val.trim().split('/');
    if (parts.length === 3) {
      const day = parseInt(parts[0], 10);
      const month = parseInt(parts[1], 10);
      const year = parseInt(parts[2], 10);
      
      // สร้าง Date object ในรูปแบบ YYYY-MM-DD สำหรับ MySQL
      // ใช้ UTC เพื่อหลีกเลี่ยงปัญหา timezone
      const date = new Date(Date.UTC(year, month - 1, day, 0, 0, 0, 0));
      return date;
    }
  }
  
  // ถ้าเป็น Date object หรือ timestamp อื่นๆ
  return new Date(val);
};
```

---

## 🧪 ทดสอบระบบ

### 1. ทดสอบการเชื่อมต่อ Google Sheets:
```powershell
node test-google-sheets.js
```

**ผลลัพธ์ที่ถูกต้อง:**
```
✓ Authorization successful
✓ Sheet found: [ชื่อชีต]

Available sheets:
  1. สาวอ้อย
  2. อลิน
  3. อัญญาC
  ...

✓ Data found!
Headers: team, B, adser, date, ...
Total rows fetched: 5
```

### 2. เริ่ม Development Server:
```powershell
npm run dev
```

### 3. ทดสอบ Sync API:
```powershell
$response = Invoke-RestMethod -Uri "http://localhost:3000/api/sync/sheets" -Method POST -Headers @{"x-api-key"="sync-sheets"; "Content-Type"="application/json"} -Body "{}"
$response | ConvertTo-Json -Depth 10
```

**ผลลัพธ์ที่ถูกต้อง:**
```json
{
  "message": "Sync completed",
  "results": {
    "success": [
      { "sheet": "สาวอ้อย", "records": 150 },
      { "sheet": "อลิน", "records": 200 },
      ...
    ],
    "failed": [],
    "totalRecords": 1500,
    "totalInserted": 1500,
    "totalUpdated": 0
  }
}
```

### 4. ตรวจสอบข้อมูลในฐานข้อมูล:
```powershell
npx prisma studio
```

เปิดตาราง `SyncData` และตรวจสอบ:
- ✅ มีข้อมูลจากทุกชีต
- ✅ คอลัมน์ `date` เป็นรูปแบบ DateTime (YYYY-MM-DD HH:mm:ss)
- ✅ ข้อมูลตัวเลขถูกต้อง (ไม่มี comma)

---

## 📋 โครงสร้างคอลัมน์ที่ถูกต้อง

ในฐานข้อมูล MySQL (`SyncData` model):

| Field | Type | Example |
|-------|------|---------|
| `date` | **DateTime** | `2024-12-25T00:00:00.000Z` |
| `team` | String | "สาวอ้อย" |
| `adser` | String | "John" |
| `message` | Int | 150 |
| `spend` | Float | 1500.50 |
| ... | ... | ... |

---

## 🔍 แก้ไขปัญหาเพิ่มเติม

### ปัญหา: "Google Sheets API has not been used"
**วิธีแก้:**
1. เปิด https://console.cloud.google.com/apis/library/sheets.googleapis.com?project=725357812362
2. คลิก "ENABLE"
3. รอ 1-2 นาที
4. ลองอีกครั้ง

### ปัญหา: "The caller does not have permission"
**วิธีแก้:**
1. ตรวจสอบว่าได้แชร์ Google Sheet กับ Service Account แล้ว
2. Email ต้องตรงกับ: `thailand-sh0424@sa-ads.iam.gserviceaccount.com`
3. ตรวจสอบว่า Sheet ID ถูกต้อง: `1Q8aPPsro7_7aqWNs13kPa3OgFp7qDRmlnXY7ve3B9hw`

### ปัญหา: รูปแบบวันที่ไม่ถูกต้อง
**วิธีแก้:**
- ตรวจสอบว่าใน Google Sheets วันที่เป็นรูปแบบ `DD/MM/YYYY`
- ตัวอย่าง: `25/12/2024` ✅
- หลีกเลี่ยง: `12-25-2024` ❌

### ปัญหา: ข้อมูลซ้ำ
**วิธีแก้:**
- ระบบใช้ `upsert` ดังนั้นข้อมูลจะถูกอัปเดตหากมีอยู่แล้ว
- Unique constraint: `team` + `adser` + `date` + `sheetName`

---

## 📝 สรุป

หลังจากทำตามขั้นตอนข้างต้น:

1. ✅ **Google Sheets API เปิดใช้งานแล้ว**
2. ✅ **Service Account มีสิทธิ์เข้าถึง Google Sheet**
3. ✅ **ฟังก์ชันแปลงวันที่ทำงานถูกต้อง (DD/MM/YYYY → YYYY-MM-DD)**
4. ✅ **ข้อมูลจะถูกบันทึกในรูปแบบ DateTime ของ MySQL**

ลองรันคำสั่งทดสอบอีกครั้ง:
```powershell
node test-google-sheets.js
```

หากสำเร็จแล้ว ให้ทดสอบ Sync API:
```powershell
$response = Invoke-RestMethod -Uri "http://localhost:3000/api/sync/sheets" -Method POST -Headers @{"x-api-key"="sync-sheets"} -Body "{}"
$response | ConvertTo-Json
```

---

## 🎯 ขั้นตอนถัดไป

หลังจากซิงค์ข้อมูลสำเร็จแล้ว คุณสามารถ:

1. **สร้าง API สำหรับดึงข้อมูล** มาแสดงใน Dashboard
2. **ตั้งค่า Cron Job** สำหรับซิงค์อัตโนมัติ
3. **เชื่อมต่อข้อมูลกับตารางใน Dashboard** แทนข้อมูลตัวอย่าง

ต้องการความช่วยเหลือเพิ่มเติมไหมครับ? 🚀
