const { PrismaClient } = require('@prisma/client')

const prisma = new PrismaClient()

async function resetSettings() {
  console.log('🔄 กำลังลบการตั้งค่าทั้งหมด...')

  try {
    const result = await prisma.userSettings.deleteMany({})
    console.log(`✅ ลบการตั้งค่าเรียบร้อยแล้ว: ${result.count} รายการ`)
    
    console.log('\n📝 หมายเหตุ:')
    console.log('- ผู้ใช้ทุกคนจะได้รับการตั้งค่าเริ่มต้นใหม่เมื่อ login')
    console.log('- สีหลัก: น้ำเงิน')
    console.log('- พื้นหลัง: กราเดี้ยนไล่เฉดเขียวมิ้นต์-ชมพู')
    console.log('- แบบอักษร: Inter')
    console.log('- ขนาดตัวอักษร: กลาง')
    
  } catch (error) {
    console.error('❌ เกิดข้อผิดพลาด:', error)
  } finally {
    await prisma.$disconnect()
  }
}

resetSettings()